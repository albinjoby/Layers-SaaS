/**
 * Next.js config (ESM)
 * Converted from next.config.ts to next.config.mjs because Next.js doesn't support
 * loading Next config from TypeScript at runtime.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  // config options here
};

export default nextConfig;
